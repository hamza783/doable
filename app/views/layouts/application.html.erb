<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Doable" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if flash[:notice] %>
      <p class="flash"><%= flash[:notice] %></p>
    <% end %>
    <div class="min-h-full">
      <nav class="bg-gray-800 shadow-sm">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 justify-between">
            <div class="flex">
              <% if Current.user %>
                <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
                  <% projects_active = request.path.include?("/projects") %>
                  <%= link_to "Projects", projects_path,
                    class: [
                      "inline-flex items-center px-1 pt-1 text-sm font-medium",
                      (projects_active ? "border-b-2 border-indigo-400 text-gray-100" : "text-gray-300 hover:text-gray-100"),
                    ].join(" ") %>
                </div>
                <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
                  <% todos_active = request.path.include?("/todos") %>
                  <%= link_to "Todos", todos_path,
                    class: [
                      "inline-flex items-center px-1 pt-1 text-sm font-medium",
                      (todos_active ? "border-b-2 border-indigo-400 text-gray-100" : "text-gray-300 hover:text-gray-100"),
                    ].join(" ") %>
                </div>
              <% end %>
            </div>
            <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
              <% if Current.user %>
                <%= button_to "Logout", session_path, method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-gray-700 text-gray-100 font-medium hover:bg-gray-600" %>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    </div>
    <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-6">
      <%= yield %>
    </main>
  </body>
</html>
